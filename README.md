# Ashish’s Enterprise Ledger

Static personal article site. Deploy this folder to GitHub Pages, Netlify, or Vercel.

Author line format: `By Ashish Deshmukh on <date>`.

Future articles should be added under `articles/`, linked from `index.html`, and included in `feed.xml` and `sitemap.xml`.
